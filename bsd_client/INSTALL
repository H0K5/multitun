#
# Multitun BSD/MacOS Client installation instructions
# Note: This is only a client, not a server

1) Install TUN support.  With mac you might be able to use this:
    http://tuntaposx.sourceforge.net/

2) Install setuptools

3) Run ./setup.py install (in this subdirectory)

4) Have to have privileges for the TUN interface (e.g. be root)

